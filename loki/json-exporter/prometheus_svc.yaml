apiVersion: v1
kind: Service
metadata:
  labels:
    run: prometheus
  name: prometheus
spec:
  ports:
  - port: 9090
    protocol: TCP
    targetPort: 9090
  selector:
    run: prometheus
  type: ClusterIP
status:
  loadBalancer: {}
